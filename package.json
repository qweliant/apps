{
  "private": true,
  "scripts": {
    "build": "pnpm p:build && turbo build",
    "dev": "pnpm p:dev && turbo dev",
    "clean": "turbo clean",
    "start": "turbo start",
    "type-check": "turbo type-check"
  },
  "dependencies": {
    "turbo": "^1.13.3"
  }
}
